import { jsxLocPlugin } from "@builder.io/vite-plugin-jsx-loc";
import tailwindcss from "@tailwindcss/vite";
import react from "@vitejs/plugin-react";
import { readFileSync } from "node:fs";
import path from "path";
import { defineConfig } from "vite";
import { vitePluginManusRuntime } from "vite-plugin-manus-runtime";

const plugins = [react(), tailwindcss(), jsxLocPlugin(), vitePluginManusRuntime()];
const cliPackageJson = JSON.parse(
  readFileSync(path.resolve(import.meta.dirname, "../cli/package.json"), "utf-8")
) as { version: string };
const maglevVersion = cliPackageJson.version;
const maglevRepositoryUrl = process.env.MAGLEV_REPOSITORY_URL ?? "";

export default defineConfig({
  define: {
    __MAGLEV_VERSION__: JSON.stringify(maglevVersion),
    __MAGLEV_REPOSITORY_URL__: JSON.stringify(maglevRepositoryUrl),
  },
  plugins,
  resolve: {
    alias: {
      "@": path.resolve(import.meta.dirname, "src"),
      "@assets": path.resolve(import.meta.dirname, "attached_assets"),
    },
  },
  build: {
    outDir: path.resolve(import.meta.dirname, "dist/public"),
    emptyOutDir: true,
  },
  server: {
    port: 3000,
    strictPort: false, // Will find next available port if 3000 is busy
    host: true,
    allowedHosts: [
      ".manuspre.computer",
      ".manus.computer",
      ".manus-asia.computer",
      ".manuscomputer.ai",
      ".manusvm.computer",
      "localhost",
      "127.0.0.1",
    ],
    fs: {
      strict: true,
      deny: ["**/.*"],
    },
  },
});
